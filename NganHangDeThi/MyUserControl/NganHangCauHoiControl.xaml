<UserControl x:Class="NganHangDeThi.MyUserControl.NganHangCauHoiControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NganHangDeThi.MyUserControl" 
             xmlns:local1="clr-namespace:NganHangDeThi.Converters" 
             xmlns:local2="clr-namespace:NganHangDeThi.Helpers"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks" 
             d:DataContext="{d:DesignInstance Type=local:NganHangCauHoiControl}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <local1:IndexConverter x:Key="IndexConverter" />
        <local1:EnumDescriptionConverter x:Key="EnumDescriptionConverter" />
        <local1:HtmlDisplayConverter x:Key="HtmlDisplayConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--Title-->
        <TextBlock Text="Quản lý câu hỏi" Grid.Row="0" 
           FontSize="32" FontWeight="SemiBold" Margin="0 0 0 4"
           HorizontalAlignment="Left" Foreground="#121518"/>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" Grid.Row="0">
                <!--Add Button-->
                <Button Background="#cccccc" Foreground="#000000" Name="BtnXoaBoLoc" Click="BtnXoaBoLoc_Click" Width="140" Style="{StaticResource AddButton}" Margin="0 0 20 0">
                    <StackPanel Orientation="Horizontal">
                        <Icon:PackIconMaterial Kind="FilterRemove" VerticalAlignment="Center" Width="11" Height="11" Margin="0 1 8 0" />
                        <TextBlock Text="Xóa bộ lọc" />
                    </StackPanel>
                </Button>
            </StackPanel>

            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <!--Filter Title-->
                <TextBlock Text="Tìm kiếm" FontFamily="13" FontWeight="SemiBold" Margin="18 0 20 0"
                        Foreground="#121518" HorizontalAlignment="Left" VerticalAlignment="Center" />

                <!--Filter Box-->
                <Grid Width="200">
                    <TextBox x:Name="TxtFilterBox" 
                             Tag="Nhập nội dung câu hỏi..."
                            Text="{Binding TuKhoaTimKiemCauHoi, UpdateSourceTrigger=PropertyChanged}" 
                            Style="{StaticResource TextBoxFilter}" />
                </Grid>
            </StackPanel>

            <Grid Grid.Row="1" Margin="0 6 0 6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!--Muc do-->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Padding="8" Text="Mức độ" FontFamily="13" FontWeight="SemiBold" Margin="12 0 10 0"
                        Foreground="#121518" HorizontalAlignment="Left" VerticalAlignment="Center" />
                    <ComboBox Style="{StaticResource ComboBoxFlatStyle}" Width="150" Margin="10 0"
                      SelectedValue="{Binding MucDoLoc, UpdateSourceTrigger=PropertyChanged}"
                      ItemsSource="{x:Static local2:MucDoCauHoiEnumHelper.DanhSach}"
                      DisplayMemberPath="Value"
                      SelectedValuePath="Key" />
                </StackPanel>

                <!--Loai-->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Loại" FontFamily="13" FontWeight="SemiBold" Margin="18 0 10 0"
                    Foreground="#121518" HorizontalAlignment="Left" VerticalAlignment="Center" />
                    <ComboBox Style="{StaticResource ComboBoxFlatStyle}" Width="150" Margin="10 0"
                      SelectedValue="{Binding LoaiLoc, UpdateSourceTrigger=PropertyChanged}"
                      ItemsSource="{x:Static local2:LoaiCauHoiEnumHelper.DanhSach}"
                      DisplayMemberPath="Value"
                      SelectedValuePath="Key" />
                </StackPanel>

                <!-- Môn học -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBlock Text="Môn học" FontFamily="13" FontWeight="SemiBold" Margin="18 0 10 0"
                    Foreground="#121518" VerticalAlignment="Center" />
                    <ComboBox Width="150" Margin="10 0"
                  ItemsSource="{Binding DsMonHoc}"
                  SelectedItem="{Binding MonHocLoc, UpdateSourceTrigger=PropertyChanged}"
                  DisplayMemberPath="TenMon"
                  Style="{StaticResource ComboBoxFlatStyle}" />
                </StackPanel>

                <!-- Chương -->
                <StackPanel Grid.Column="3" Orientation="Horizontal">
                    <TextBlock Text="Chương" FontFamily="13" FontWeight="SemiBold" Margin="18 0 10 0"
                        Foreground="#121518" VerticalAlignment="Center" />
                    <ComboBox Width="150" Margin="10 0"
                      ItemsSource="{Binding DsChuong}"
                      SelectedItem="{Binding ChuongLoc, UpdateSourceTrigger=PropertyChanged}"
                      DisplayMemberPath="TenChuong"
                      Style="{StaticResource ComboBoxFlatStyle}" />
                </StackPanel>
            </Grid>

            <DataGrid Grid.Row="2" ItemsSource="{Binding CauHoiView}" CanUserSortColumns="False"
                  RowStyle="{DynamicResource DataGridRowStyle1}" 
                  ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" 
                  CellStyle="{DynamicResource DataGridCellStyle1}" 
                  Style="{DynamicResource DataGridStyle1}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" 
                                        Binding="{Binding Id}" 
                                        Visibility="Collapsed" IsReadOnly="True" CanUserResize="False" Width="auto" />

                    <DataGridTextColumn Header="Nội dung" 
                                        Binding="{Binding NoiDung, Converter={StaticResource HtmlDisplayConverter}}" 
                                        IsReadOnly="True" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextWrapping" Value="Wrap" />
                                <Setter Property="MaxHeight" Value="100" />
                                <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                <Setter Property="Padding" Value="5"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Mức độ" Binding="{Binding MucDo, Converter={StaticResource EnumDescriptionConverter}}" IsReadOnly="True" Width="auto" />
                    <DataGridTextColumn Header="Loại" Binding="{Binding Loai, Converter={StaticResource EnumDescriptionConverter}}" IsReadOnly="True" Width="auto" />

                    <DataGridTemplateColumn Header="Thao tác" IsReadOnly="True" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="10 0">
                                    <Button Name="BtnSuaCauHoi" Click="BtnSuaCauHoi_Click" Style="{StaticResource GridEditButton}" Tag="{Binding}" ToolTip="Sửa" Margin="0 0 5 0">
                                        <Icon:PackIconMaterial Kind="PencilOffOutline" Style="{StaticResource GridButtonIcon}" />
                                    </Button>
                                    <Button Name="BtnXoaCauHoi" Click="BtnXoaCauHoi_Click" Style="{StaticResource GridRemoveButton}" Tag="{Binding}" ToolTip="Xóa" Margin="5 0 0 0">
                                        <Icon:PackIconMaterial Kind="DeleteOffOutline" Style="{StaticResource GridButtonIcon}" />
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel HorizontalAlignment="Left" Grid.Column="0">
                <TextBlock FontSize="16" FontWeight="Light" Foreground="#121518" Padding="10" Margin="0 0 20 0">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="Hiển thị: {0}/{1} câu hỏi">
                            <Binding Path="TongDangHienThi"/>
                            <Binding Path="TongTatCa"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Column="1">
                <!--Delete Selected Question Button-->
                <!--<Button Name="BtnXoaCauHoiDaChon" Click="BtnXoaCauHoiDaChon_Click" Background="IndianRed" Width="140" Style="{StaticResource AddButton}" Margin="0 0 4 0" >
                    <StackPanel Orientation="Horizontal">
                        <Icon:PackIconMaterial Kind="Plus" VerticalAlignment="Center" Width="11" Height="11" Margin="0 1 8 0" />
                        <TextBlock Text="Xóa câu hỏi đã chọn" />
                    </StackPanel>
                </Button>-->
                <!--Refresh Button-->
                <Button Name="BtnTaiLaiCauHoi" Click="BtnTaiLaiCauHoi_Click" Style="{StaticResource AddButton}" Margin="0 0 4 0">
                    <StackPanel Orientation="Horizontal">
                        <Icon:PackIconMaterial Kind="Refresh" VerticalAlignment="Center" Width="11" Height="11" Margin="0 1 8 0" />
                        <TextBlock Text="Tải lại" />
                    </StackPanel>
                </Button>
                <!--Them cau hoi thu cong-->
                <Button Name="BtnThemThuCong" Click="BtnThemThuCong_Click" Width="120" Style="{StaticResource AddButton}" Margin="0 0 4 0">
                    <StackPanel Orientation="Horizontal">
                        <Icon:PackIconMaterial Kind="PencilPlus" VerticalAlignment="Center" Width="11" Height="11" Margin="0 1 8 0" />
                        <TextBlock Text="Thêm thủ công" />
                    </StackPanel>
                </Button>
                <!--Add From File Button-->
                <Button Name="BtnThemCauHoiTuFile" Click="BtnThemCauHoiTuFile_Click" Style="{StaticResource AddButton}" Margin="0 0 20 0">
                    <StackPanel Orientation="Horizontal">
                        <Icon:PackIconMaterial Kind="File" VerticalAlignment="Center" Width="11" Height="11" Margin="0 1 8 0" />
                        <TextBlock Text="Thêm từ file" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
